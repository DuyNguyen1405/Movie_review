<%= form_for(@favorite_movie) do |f| %>
  <% if @favorite_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favorite_movie.errors.count, "error") %> prohibited this favorite_movie from being saved:</h2>

      <ul>
      <% @favorite_movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
